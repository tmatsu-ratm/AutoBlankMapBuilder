<Window x:Class="AutoBlankMapBuilder.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoBlankMapBuilder.Views"
        mc:Ignorable="d"
        Title="AutoBlankMapBuilder" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid Column="0">
                    <Button x:Name="Button1" Content="設定" Click="Button1_OnClick" Margin="20 5"/>
                </Grid>
                <Grid Column="1">
                    <Button x:Name="Button2" Content="作成" Click="Button2_OnClick" Margin="20 5"/>
                </Grid>
            </Grid>
            <TextBox x:Name="LogText" AcceptsReturn="True" Height="300" Margin="5" VerticalScrollBarVisibility="Auto" BorderBrush="DimGray" BorderThickness="1" />
            <Label x:Name="LabelTimer" />
            <Label x:Name="LabelTimer2"/>
        </StackPanel>
        
    </Grid>
</Window>
